# Cloudflare Prometheus Exporter – Setup & API Docs

This Prometheus exporter connects to the Cloudflare GraphQL API to collect zone-level, account-level, and firewall metrics. It exposes them in the `/metrics` endpoint for Prometheus to scrape.

This document covers prerequisites, installation steps using Docker, available configuration options, and example usage.


